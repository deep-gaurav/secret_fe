targets:
  $default:
    builders:
      artemis:
        options:
          schema_mapping:
            - output: lib/api/api.dart
              schema: lib/schema.graphql
              queries_glob: lib/graphql/**.graphql
          scalar_mapping:
            - graphql_type: UUID
              dart_type: String